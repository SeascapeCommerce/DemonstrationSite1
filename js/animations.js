/* =========================================================
   SEASCAPE GLOBAL ANIMATIONS
   ========================================================= */

(() => {

    "use strict";

    if (typeof gsap === "undefined") {
        return;
    }

    /*
       Global animations will be added here as
       the rest of the Seascape experience develops.

       Agent-network-specific animation remains
       inside agent-network.js.
    */

})();
